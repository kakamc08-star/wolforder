# قسم طلبات Instagram في WolfOrder

هذه النسخة تضيف مساحة Instagram داخل WolfOrder مع فصل كامل للطلبات والأصناف والمخزون والجرد عن الطلبات الأساسية.

## قواعد ثابتة

- نوع طلب Instagram الجديد هو `توصيل` فقط.
- سجلات الشحن القديمة لا تُحذف، لكنها لا تظهر في لوحات Instagram ولا تُقبل من الرابط العام.
- طلبات Instagram لا تُحفظ في جدول `orders` الأساسي.
- طلبات `توصيل` فقط يمكن تعيينها للسائق وتظهر في لوحة السائق الحالية.
- رابط الزبون لا يعرض كمية المخزون، ويعيد الخادم فحص المخزون ويحجزه داخل معاملة قاعدة بيانات آمنة.
- رقم العميل في الرابط العام يُنظّف من الرموز ويدعم الأرقام العربية والفارسية، ثم يُقبل إذا كان 10 أرقام إنجليزية بالضبط.
- تعديل الطلب من لوحة المدير يتم في معاملة واحدة تشمل البيانات والأصناف وأثر المخزون.
- أثناء إعادة حفظ الأصناف يترك النظام عمود `line_total` المحسوب لقاعدة البيانات؛ لا تتم محاولة إدخال قيمته يدوياً.
- حساب مشاهدة Instagram مربوط بشركة واحدة ويملك صلاحية القراءة فقط لطلبات وجرد تلك الشركة.

## الصفحات الجديدة

- `/instagram-admin.html`: إدارة طلبات Instagram والأصناف والجرد والروابط وحسابات المشاهدة.
- `/o/:slug`: نموذج طلب الزبون العام والسريع.
- `/instagram-viewer.html`: حساب مشاهدة الشركة.

## النشر

1. يجب أن تكون جداول ودوال Instagram في Supabase مطبقة قبل نشر هذه النسخة.
2. يجب تشغيل `database/instagram-order-edit.sql` مرة واحدة لإضافة دالة التعديل الذري وحارس نوع الطلب.
3. تُستخدم متغيرات البيئة الحالية نفسها: `SUPABASE_URL` و`SUPABASE_SERVICE_ROLE_KEY` و`JWT_SECRET`.
4. شغّل `npm ci` ثم `npm start` بالطريقة الحالية للمشروع.
5. بعد دخول المدير افتح «طلبات الإنستغرام»، وأنشئ الأصناف والرابط وحساب المشاهدة عند الحاجة.

لا يحتوي هذا المشروع على ملف `.env` داخل نسخة التسليم.
