# نشر تحديث طلبات إنستغرام

## الترتيب الصحيح

1. خذ نسخة احتياطية من قاعدة Supabase من لوحة المشروع كإجراء احترازي.
2. افتح **Supabase → SQL Editor → New query**.
3. للتشغيل دفعة واحدة، انسخ كامل الملف `database/instagram-all-migrations-20260908.sql` والصقه ثم اضغط **Run**. هذا الملف يجمع ترحيلات الشحن والمخزون والحجز وتعديل الطلب وتبديل النوع والتحقق من المدخلات بالترتيب الصحيح.
4. بعد نجاحه، تأكد من ظهور الجداول التي تبدأ بـ `instagram_` ومن نجاح إنشاء الدوال الجديدة.
5. حدّث متغيرات البيئة في Render بالقيم الموضحة في `.env.example`، مع إبقاء قيم Supabase وRasel الحالية كما هي.
6. انشر ملفات المشروع الجديدة على Render ثم افتح `/login.html` وسجّل الدخول كمدير.
7. من قائمة المدير افتح **الطلبات القادمة من الإنستغرام**، ثم أنشئ رابط الشركة وأضف الأصناف والكميات.
8. أنشئ حساب مشاهدة مستقل للشركة من الصفحة نفسها عند الحاجة.

### التشغيل المنفصل (بديل)

إذا لم تستخدم الملف الجامع، شغّل الملفات التالية بالترتيب بعد ترحيلات Instagram الأساسية (`2026-08-27-instagram-orders-and-performance.sql` و`2026-08-28-admin-parity-and-safe-deletions.sql`):

1. `database/2026-09-04-instagram-shipping-workflow.sql`
2. `database/2026-09-05-instagram-inventory-release-fix.sql`
3. `database/2026-09-05-instagram-shipping-pending-reservation.sql`
4. `database/2026-09-08-instagram-order-edit-type-and-validation.sql`

لا تشغّل الملف الجامع والملفات الأربعة منفردة بعده؛ اختر طريقة واحدة فقط.

لا يحذف التحديث سجلات الشحن القديمة من قاعدة البيانات؛ يتم استبعادها من واجهات ومسارات Instagram الحالية حتى تبقى محفوظة للتاريخ.

## متغيرات البيئة الجديدة

- `REFRESH_TOKEN_SECRET`: سر عشوائي طويل ومختلف لتجديد الجلسة.
- `FORM_TOKEN_SECRET`: سر عشوائي طويل لتوقيع نموذج الطلب العام.
- `IP_HASH_SECRET`: سر عشوائي طويل لتجزئة عنوان مصدر الطلب في حماية المعدل.

إذا لم تُضف هذه القيم سيستخدم النظام `JWT_SECRET` مؤقتاً، لكن يوصى بشدة بوضع قيمة مختلفة لكل متغير قبل النشر.

## ملاحظات مهمة

- لا تنفّذ ملف SQL القديم `2026-08-23-add-order-type.sql` مرة ثانية إذا كان مطبقاً مسبقاً؛ هو موجود فقط ضمن تاريخ المشروع.
- ملفا Migration لا يحذفان الطلبات أو المستخدمين أو الجداول الحالية أثناء التثبيت.
- طبّق ملفي Migration قبل نشر الكود لأن الواجهة الجديدة تعتمد دوال الحذف الآمن وحقول الأرشفة.
- ملف `database/instagram-order-edit.sql` هو Migration تاريخي لأساس التعديل. الملف `2026-09-08-instagram-order-edit-type-and-validation.sql` إلزامي بعده لأنه يستبدل دالة التعديل القديمة ويفتح تعديل الشحن وتبديل النوع ويزيل حارس التوصيل فقط.
- إذا ظهرت رسالة `cannot insert a non-DEFAULT value into column "line_total"` أو `instagram_inventory_movement_type_check` أو بقي إنشاء الطلب متعذراً، شغّل `database/2026-09-04-instagram-shipping-workflow.sql` من أوله؛ الترحيل يترك العمود المحسوب لـ PostgreSQL ويوسّع قيد حركات Instagram للأنواع الجديدة.
- إذا بقيت كمية «المتبقية» قديمة بعد حذف طلب، تأكد من تشغيل `database/2026-09-05-instagram-inventory-release-fix.sql` بعد Migration الشحن، ثم حدّث الصفحة مرة واحدة لتثبيت نسخة JavaScript الجديدة.
- طلبات الشحن الجديدة تحجز المخزون قبل موافقة الشركة؛ القبول لا يخصم مرة ثانية، والرفض يعيد الحجز ثم يحذف الطلب. الطلبات القديمة المعلقة التي تتجاوز المخزون المتاح ستبقى معلقة حتى يتم رفضها أو إعادة توفير الكمية.
- حالة الشحن المعروضة هي `لم يتم تسليم اسم الشركة` أو `تم تسليم اسم الشركة` مع تاريخ ووقت التسليم. هذه العملية لا تغيّر أي حجز مخزون.
- الهاتف يُحفظ بصيغة محلية تبدأ بـ `0` وتتكون من 10 أرقام، والعنوان لا يقل عن 3 محارف، والملاحظة لا تتجاوز 85 محرفاً. الطلبات القديمة لا تُعاد كتابتها تلقائياً؛ التحقق يطبق على الإدخالات والتعديلات الجديدة.
- ملف ZIP لا يحتوي `.env` الحقيقي أو مجلد `.git` أو `node_modules`. انسخ قيم البيئة الحالية إلى Render ولا تضع الأسرار داخل المشروع.
- تصدير Excel ينتج ملف SpreadsheetML بامتداد `.xls` ويدعم صفحة ملخص وصفحة لكل صنف من دون مكتبات إضافية.

## فحص سريع بعد النشر

1. افتح رابط شركة وأرسل طلباً تجريبياً بقطعة واحدة.
2. تأكد أن الكمية انتقلت من المتبقي إلى المحجوز.
3. غيّر الحالة إلى `تم` وتأكد أنها أصبحت مباعة.
4. غيّر طلباً تجريبياً آخر إلى `إلغاء` أو `مرتجع` وتأكد من عودة الكمية مرة واحدة.
5. ادخل بحساب `instagram_viewer` وتأكد من عدم ظهور أي أزرار تعديل أو تصدير ومن رفض روابط API المحمية.
6. اختبر لوحة السائق وتأكد من ظهور الطلبات الأساسية وطلبات إنستغرام في الجدول نفسه.
7. اختبر رقم الهاتف بالأرقام العربية والفارسية والنسخ واللصق، ثم اختبر تعديل البيانات والأصناف وإعادة تحميل الصفحة.
8. اختبر فلتر `بدون سائق`، وتعديل طلب شحن، وتبديل النوع في الاتجاهين مع زيادة/إنقاص صنف، وتأكد من أن الفرق فقط تغيّر المخزون.
9. اختبر الحذف والطباعة وتعيين السائق والتحديد الجماعي، وتأكد من ظهور مصدر الطلب كشارة زرقاء للطلبات الأساسية وعنابية لطلبات Instagram في لوحة السائق.
