# نشر تحديث طلبات إنستغرام

## الترتيب الصحيح

1. خذ نسخة احتياطية من قاعدة Supabase من لوحة المشروع كإجراء احترازي.
2. افتح **Supabase → SQL Editor → New query**.
3. انسخ كامل الملف `database/2026-08-27-instagram-orders-and-performance.sql` وشغّله مرة واحدة.
4. بعده مباشرة شغّل `database/2026-08-28-admin-parity-and-safe-deletions.sql` لتفعيل الحذف الآمن وتحديث الجرد.
5. تأكد من ظهور الجداول التي تبدأ بـ `instagram_` ومن نجاح إنشاء الفهارس والدوال الجديدة.
6. حدّث متغيرات البيئة في Render بالقيم الموضحة في `.env.example`، مع إبقاء قيم Supabase وRasel الحالية كما هي.
7. انشر ملفات المشروع الجديدة على Render ثم افتح `/login.html` وسجّل الدخول كمدير.
8. من قائمة المدير افتح **الطلبات القادمة من الإنستغرام**، ثم أنشئ رابط الشركة وأضف الأصناف والكميات.
9. أنشئ حساب مشاهدة مستقل للشركة من الصفحة نفسها عند الحاجة.
10. لتسليم الشحن: افتح تبويب **طلبات الشحن**، حدّد الطلبات، ثم اضغط **تجميع المحدد وتسليمه للشريكة** وأدخل اسم الشريكة.

## متغيرات البيئة الجديدة

- `REFRESH_TOKEN_SECRET`: سر عشوائي طويل ومختلف لتجديد الجلسة.
- `FORM_TOKEN_SECRET`: سر عشوائي طويل لتوقيع نموذج الطلب العام.
- `IP_HASH_SECRET`: سر عشوائي طويل لتجزئة عنوان مصدر الطلب في حماية المعدل.

إذا لم تُضف هذه القيم سيستخدم النظام `JWT_SECRET` مؤقتاً، لكن يوصى بشدة بوضع قيمة مختلفة لكل متغير قبل النشر.

## ملاحظات مهمة

- لا تنفّذ ملف SQL القديم `2026-08-23-add-order-type.sql` مرة ثانية إذا كان مطبقاً مسبقاً؛ هو موجود فقط ضمن تاريخ المشروع.
- ملفا Migration لا يحذفان الطلبات أو المستخدمين أو الجداول الحالية أثناء التثبيت.
- طبّق ملفي Migration قبل نشر الكود لأن الواجهة الجديدة تعتمد دوال الحذف الآمن وحقول الأرشفة.
- ملف ZIP لا يحتوي `.env` الحقيقي أو مجلد `.git` أو `node_modules`. انسخ قيم البيئة الحالية إلى Render ولا تضع الأسرار داخل المشروع.
- تصدير Excel ينتج ملف SpreadsheetML بامتداد `.xls` ويدعم صفحة ملخص وصفحة لكل صنف من دون مكتبات إضافية.

## فحص سريع بعد النشر

1. افتح رابط شركة وأرسل طلباً تجريبياً بقطعة واحدة.
2. تأكد أن الكمية انتقلت من المتبقي إلى المحجوز.
3. غيّر الحالة إلى `تم` وتأكد أنها أصبحت مباعة.
4. غيّر طلباً تجريبياً آخر إلى `ملغي` أو `مرتجع` وتأكد من عودة الكمية مرة واحدة.
5. ادخل بحساب `instagram_viewer` وتأكد من عدم ظهور أي أزرار تعديل أو تصدير ومن رفض روابط API المحمية.
6. اختبر لوحة السائق وتأكد من ظهور الطلبات الأساسية وطلبات إنستغرام في الجدول نفسه.
7. اختبر التعديل والحذف والطباعة وتعيين السائق والتحديد الجماعي من لوحة إنستغرام.
